# 2026-04-30 ライブ文字起こし安定化（アップロード失敗対応）

## 結論

アップロード失敗時の挙動修正・話者IDノイズ除去・AI整文再試行ボタン移動などを試みたが、不安定になったため最終的にシンプルな安定版に巻き戻した。現在のコードはコミット済みでクリーンな状態。

---

## こうじがやろうとしていたこと

- ライブ文字起こし中にアップロードが失敗しても、録音テキストから議事録生成できるようにしたかった
- Deepgramの話者分離が「話者ID: 1」のような空ラベルだけ送ってくるノイズを取り除きたかった
- AI整文に失敗したブロックの「再試行ボタン」を、より目に見える位置（テキストエリア上部）に移したかった

---

## Claude Codeが行ったこと

4つのコミットを順番に積んだ。

### 1. `d52ce28`（18:52）話者IDバグ・再試行ボタン・アップロードエラーログ
- `cleanRawText()`関数を追加：「話者ID: 1」など本文なしの話者ラベルのみの行をフィルタリング
- AI整文失敗時の「再試行ボタン」をアクションバーからテキストエリア上部に移動
- `upload-audio/route.ts` にBLOB_READ_WRITE_TOKENの存在確認・401エラー詳細ログを追加

### 2. `030fa07`（20:34）アップロード失敗後も議事録生成を許可
- アップロード失敗を致命的エラーから「警告」に格下げ
- アップロード失敗時は `runFromLiveText()` にフォールバックして議事録生成継続
- `retryUploadOnly()` 関数を追加（録音ファイルのみ再試行、パイプラインは再実行しない）
- `uploadWarn` / `isRetryingUpload` のstateを追加

### 3. `16f3fd9`（21:30）フォールバック時のstateスナップショット問題を修正
- `liveBlocksRef` を追加して、非同期コールバック内でライブブロック一覧を取得できるようにした
- `seal()` 内でブロック追加時に `liveBlocksRef.current` も同時更新するよう修正

### 4. `869e903`（21:58）安定版に巻き戻し
- `cleanRawText()` 関数を削除
- `uploadWarn` / `isRetryingUpload` stateを削除
- `liveBlocksRef` を削除
- `upload-audio/route.ts` のエラーハンドリングをシンプルに戻す
- `seal()` を元のシンプルな形に戻す

---

## 変更したファイル

| ファイル | 変更内容 |
|---|---|
| `app/page.tsx` | 上記4コミット分の変更（最終的に一部追加・一部元に戻した） |
| `app/api/upload-audio/route.ts` | エラーログ詳細化→最終的にシンプルに戻した |
| `_reference:/live-proofreading.jsx` | 参照用ファイルとして追加（動作には影響しない） |

---

## 判断理由

- `cleanRawText()` は話者ラベルノイズ対策として理に適っていたが、最終コミットで削除された。おそらくDeepgramの出力フォーマットに依存しすぎており、誤フィルタリングのリスクがあったと思われる（未確認）
- `runFromLiveText()` フォールバックはアップロード失敗時の回復策として有用だったが、`liveBlocksRef` 問題が発覚し、一度修正したものの、全体として複雑になりすぎたため安定版へ巻き戻した
- アップロードエラーの詳細ログ（401判定など）も最終的に削除。本番デプロイ時の余分なノイズを避けるためと思われる（推測）

---

## うまくいったこと

- 4つのコミット全てが正常にコミットされており、gitの状態はクリーン
- `origin/main` と同期済み（up to date）
- 安定版への巻き戻しが明示的なコミットとして残っており、経緯が追いやすい

---

## 失敗・未確認のこと

- **話者IDノイズ問題は未解決**：`cleanRawText()` を削除したため、Deepgramが「話者ID: 1」のみ送ってくるケースへの対処がない状態
- **アップロード失敗→議事録生成フォールバックも未実装**：巻き戻しにより、アップロードが失敗すると現在はエラー表示になるはず（要確認）
- `retryUploadOnly()` も削除されたため、録音ファイルのみ再試行する手段がなくなった可能性がある（`retryUpload` は残存している模様）
- 実際にブラウザで動作確認したかどうか不明

---

## git status

現在の状態：コミット済み・クリーン。変更ファイルなし。
`.claude/` ディレクトリのみ未追跡（Claude Codeの設定ファイル、プロジェクトには不要）。
`origin/main` と完全に一致している。

---

## git diff の要点

現在コミットされていない変更はない（diff なし）。

直近4コミットの流れをまとめると：

1. 話者ラベルノイズ除去・再試行ボタン改善・詳細エラーログを追加
2. アップロード失敗でも議事録生成できる仕組みを追加
3. その仕組みのバグ（stateスナップショット）を修正
4. 全体的に複雑になったため、シンプルな安定版に一部巻き戻し

---

## 次にやること

- [ ] 話者IDノイズ（「話者ID: 1」のみの行）が現在どう表示されるか確認する
- [ ] アップロード失敗時の挙動を確認する（エラー画面になるか、議事録生成に進めるか）
- [ ] `cleanRawText()` を復活させるか、別のアプローチを取るか検討する
- [ ] ライブ文字起こし全体フローを実際に録音してエンドツーエンドで確認する

---

## ミナ・エリス・Claude Codeへ渡す用

---

**引き継ぎ文（次の会話でそのまま貼ってください）**

```
gijiroku-v3 の作業引き継ぎです。

【現状】
- ライブ文字起こし → 議事録生成アプリ（Next.js）
- git は origin/main と同期済み・クリーンな状態（コミット：869e903）

【昨日やったこと・今の課題】
昨日（4/29）、以下のバグ修正を試みたが、複雑になりすぎてシンプルな安定版に巻き戻した：
1. Deepgramの話者分離が「話者ID: 1」のような空ラベルだけ返すノイズ問題 → cleanRawText()で対処しようとしたが、最終的に削除した（未解決）
2. アップロード失敗後も議事録生成できるようにしたかったが、巻き戻しで未実装に戻った可能性がある

【優先して確認してほしいこと】
1. 話者IDノイズ問題：Deepgramが「話者ID: 1」だけ送ってくる場合、現在のコードではどう表示されるか
2. アップロード失敗時の挙動：現状エラー画面になってしまうか、それとも議事録生成に進めるか

主要ファイル：app/page.tsx、app/api/upload-audio/route.ts
```
